
// Force compileSdk 35 for every Android module
subprojects {
  plugins.withId("com.android.library") {
    android { compileSdk 35 }
  }
  plugins.withId("com.android.application") {
    android { compileSdk 35 }
  }
}
